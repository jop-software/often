<!DOCTYPE html>
<html lang="en">
    <head>
        <title>OFTEN</title>

        {# TODO: HTTP 404 on all references to /vendor/ #}

        {# Include jQuery #}
        <script src="{{ base }}/vendor/components/jquery/jquery.js.min"></script>

        {# Include Bootstrap #}
        <link rel="stylesheet" href="{{ base }}/vendor/components/bootstrap/css/bootstrap.css">
        <script src="{{ base }}/vendor/components/bootstrap/js/bootstrap.js"></script>
    </head>
    <body>
        {% block navigation %}
            {% include "components/navigation.twig" %}
        {% endblock %}

        {% block content %}{% endblock %}
    </body>
</html>