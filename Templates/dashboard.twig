{% extends "index.twig" %}

{% block content %}
    <div class="row">
        <div class="col-lg-12 col-sm-12">
            <h2 class="text-center">{{ user.getUsername() }}'s Dashboard</h2>
        </div>
    </div>
{% endblock %}